# /cycle-plan

Cycle planning is disabled for this agent.

Do not plan, create, update, assign, or remove Linear Cycles. Do not write `cycleId` in Linear write plans. If this prompt is invoked, explain that cycle functionality is disabled and continue only with Project, Milestone, Issue, dependency, or report work that does not use Cycles.

User input:
{{input}}
